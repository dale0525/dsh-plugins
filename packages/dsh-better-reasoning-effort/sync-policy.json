{
  "_comment": [
    "本包的上游同步策略。scripts/sync-upstream.mjs 据此把上游新版本合进本仓库的 fork。",
    "顺序铁律：先 checkout --theirs（全取上游）→ 再按 pull 之前的 commit 恢复 owned（非 --ours，--ours 对干净合并是空操作）→ 再 git rm（重删 deleted）。",
    "顺序反了会让我们的改造被上游覆盖。",
    "本文件由 scripts/sync-upstream.mjs --refresh-policy --target <target.id> 按当前 fork 状态生成；",
    "手工改了本包的文件集后必须重跑，否则 deleted 清单会漏项（上游会静默复活我们删掉的代码）。",
    "路径均为包相对（相对 target.prefix）。"
  ],
  "target": {
    "id": "better-reasoning-effort",
    "url": "https://github.com/HaoyueQin/dsh-better-reasoning-effort",
    "prefix": "packages/dsh-better-reasoning-effort",
    "baseline": "v0.4.1",
    "baselineCommit": "3d0175bc4236651d175691330b260839fa0c2e63"
  },
  "owned": [
    "cordis.patch.yml",
    "package.json",
    "src/client/index.ts",
    "src/client/injection/autofill-run.ts",
    "src/client/ops.ts",
    "src/client/types.ts",
    "src/constants.ts",
    "src/index.ts",
    "tests/client.spec.tsx",
    "tests/host.spec.ts",
    "tests/ops.spec.ts",
    "tests/setup.ts",
    "tests/support/composer-fixture.ts"
  ],
  "deleted": [
    ".github/workflows/ci.yml",
    ".github/workflows/release.yml",
    ".github/workflows/sync-to-gitee.yml",
    ".npmrc",
    "package-lock.json"
  ],
  "added": [
    "tests/support/refs.ts"
  ]
}
