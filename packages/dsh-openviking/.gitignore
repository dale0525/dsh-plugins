# Generated by ../dsh-openviking-shared/sync.mjs — the transitive closure of what
# this plugin imports from the shared library. Rebuilt by `prepare` / `prepack`,
# which is also what puts it in the published tarball; `files` in package.json
# still lists `shared/`, so npm ships the built copies without git tracking them.
/shared/
